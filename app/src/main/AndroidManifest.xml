<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.onlywatch.fleriche.onlywatch">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".general.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".general.ConsultActivity" />
        <activity
            android:name=".test.HeroesActivity"
            android:label="@string/title_activity_heroes"
            android:theme="@style/AppTheme" />
        <activity android:name=".test.HeroesFlipActivity" />
        <activity android:name=".general.ConsultActivity" />
        <activity android:name=".heroes.HeroesFilterActivity" />
        <activity android:name=".general.SettingsActivity" />
        <activity android:name=".general.HelpActivity" />
        <activity android:name=".maps.MapActivity" />
        <activity android:name=".favorite.FavoriteActivity" />
        <activity android:name=".maps.MapsFilterActivity"></activity>
    </application>

</manifest>